# Phase 5b Opus Review — 2026-06-25

## Findings (ranked most severe first)

1. **MEDIUM** `server/src/routes/sleep.ts:88` — `morningBlocks` filter does not exclude blocks already stamped `rescheduled: true`. A duplicate POOR POST (network retry, double-submit) re-derives the same morning blocks (still ACTIVE), re-returns `blocksToDelete`, and the client attempts to delete already-deleted calendar events. Expo Calendar throws on `deleteEventAsync` for a non-existent event. Fix: add `!b.rescheduled` to the filter.

2. **LOW** `app/src/hooks/use-sleep-sync.ts:11,31,41,70,88,99,124` — Multiple multi-line comment blocks violate CLAUDE.md "Never write multi-paragraph docstrings or multi-line comment blocks — one short line max." The WHY content in each is non-obvious and should be preserved, but each must be collapsed to a single line.

3. **LOW** `server/src/routes/sleep.ts:9,17,46,66,87,102,131` and `server/src/lib/sleep-reschedule.ts:110,120` — WHAT-only comments describe what the immediately following code does; CLAUDE.md says "Only add one when the WHY is non-obvious." These should be removed.

4. **LOW** `server/src/routes/sleep.ts:93,84` and `server/src/lib/sleep-reschedule.ts:132` — Legitimate WHY comments spanning two lines. Collapse each to one line per CLAUDE.md.

## Candidates examined and refuted

- **utcToLocalHour small-icu ICU bug** — REFUTED. The exact same `formatToParts` + `parseInt % 24` pattern is already used in `localTimeToUtc` (proven working since Phase 4b). Node.js on Windows ships full ICU by default.
- **`now` captured before DB reads** — REFUTED. In current code `const now = new Date()` appears AFTER the `Promise.all` on line 75 completes.
- **`busySlots` includes past elapsed blocks** — REFUTED. `freeGaps` filter `b.endTime > windowStart` excludes all elapsed blocks.

## Verdict: 1 medium fix + comment cleanup required
