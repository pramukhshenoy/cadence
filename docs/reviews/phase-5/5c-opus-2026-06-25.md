# Phase 5c Opus Review — 2026-06-25

```json
[
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 51,
    "summary": "FOCUS_WEEK_SUMMARY_KEY is never invalidated when blocks are archived but no new blocks can be scheduled (droppedCount > 0 or all writeFocusBlock calls fail)",
    "failure_scenario": "Morning blocks archived (archiveFocusBlocks called), but afternoon is full (newBlocks=[]) or all writeFocusBlock calls fail (toSave=[]). Line 51 returns early / line 64 skips — queryClient.invalidateQueries is never called. Dashboard focus widget continues to show the now-deleted blocks as scheduled focus time until next calendar sync."
  },
  {
    "file": "server/src/routes/sleep.ts",
    "line": 118,
    "summary": "Map + second .map() is unnecessary — morningBlocks.map(b => {id, deviceCalendarEventId}) directly produces the same result",
    "failure_scenario": "No current failure. But the Map indirection introduces an implicit assumption (blocksToDelete == morningBlocks.map(b=>b.id)) that calendarIds.get(id)??'' would silently hide if that ever diverges. Direct mapping makes the assumption explicit and removes the intermediate allocation."
  }
]
```

## Candidates examined and refuted

- **deviceCalendarEventId null in DB** — REFUTED. `data-model.md` declares it `String @unique` (non-nullable). The `?? ''` fallback is unreachable dead code.
- **busySlots including past-day blocks restricting windows** — REFUTED. `freeGaps`'s `b.endTime > windowStart` filter naturally excludes all past-ended blocks before the gap loop.
- **capturedSummary alias** — PLAUSIBLE minor cleanup but not filed; `summary` is already narrowed and the alias is harmless noise.

## Verdict: 1 medium fix + 1 simplification required
