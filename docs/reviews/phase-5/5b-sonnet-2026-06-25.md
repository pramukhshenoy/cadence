# Phase 5b Sonnet Review — 2026-06-25

## Findings (ranked most severe first)

1. **MEDIUM** `server/src/lib/sleep-reschedule.ts:113` — `droppedCount` uses `Math.floor(remainingMinutes / 60)` which returns 0 when 30–59 min remain unscheduled — any partial block is silently lost with no signal to the caller.

2. **MEDIUM** `server/src/routes/sleep.ts:90` — `POST /api/sleep` identifies morning blocks to displace but never stamps `rescheduled=true` / `rescheduleReason` in DB before returning the plan. Client crash between POST and batch-delete leaves those blocks ACTIVE forever; Phase 4c sync later tries to delete non-existent calendar events.

3. **LOW** `server/src/routes/sleep.ts:56` — `sessionCount: null` returns 400 instead of defaulting to 1. `deepSleepHours: null` and `remSleepHours: null` both pass validation; sessionCount is inconsistent. The `?? 1` fallback in the upsert is never reached for null.

4. **LOW** `server/src/lib/sleep-reschedule.ts:4` — `MIN_BLOCK_MINUTES` and `BLOCK_DURATION_MINUTES` re-declared instead of imported from `scheduler.ts` where they already exist (lines 34–35, non-exported). Divergence risk if minimum block size changes.

5. **LOW** `server/src/lib/sleep-reschedule.ts:45` — `freeGaps` and `fillGaps` duplicate the gap-sweep and greedy-fill loops from `scheduler.ts scheduleBlocks` (lines 122–159). Bug fixes must be applied in two places.

6. **LOW** `server/src/routes/sleep.ts:8` — `utcToLocalParts` duplicates `utcToLocalDateStr` from `scheduler.ts` (line 37, non-exported) with a second `Intl.DateTimeFormat` instance. DST fix applied to one silently diverges the other.

## Verdict: 6 fixes required (2 medium, 4 low)
