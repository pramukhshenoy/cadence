# Phase 5c Sonnet Review — 2026-06-25

```json
[
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 38,
    "summary": "getActiveFutureBlocks() excludes already-started morning blocks (startTime < now), so blocksToDelete from the plan never match and calendar deletion silently no-ops",
    "failure_scenario": "User opens app at 9:30am. A 9am focus block (startTime < now) is in plan.blocksToDelete. GET /api/focus-blocks filters startTime >= now, so that block is absent from activeBlocks. deleteIdSet.has(b.id) never matches, deleteCalendarEvent is never called, archiveFocusBlocks is never called. The block stays ACTIVE in DB and visible in calendar."
  },
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 38,
    "summary": "getActiveFutureBlocks() is called before postSleepRecord — a network failure here suppresses the sleep record POST entirely",
    "failure_scenario": "getActiveFutureBlocks() throws (server down). catch block runs, resets RESCHEDULE_ATTEMPTED_KEY. postSleepRecord is never called — sleep data is lost for this session mount. Correct fix: call postSleepRecord first; only call the block-fetching step if plan.blocksToDelete.length > 0."
  },
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 44,
    "summary": "Early exit on blocksToDelete.length === 0 leaves user with no focus blocks after a partial-failure retry",
    "failure_scenario": "Backend stamped morningBlocks as rescheduled=true and returned a plan, but the client crashed before writing calendar events. On retry, server filters !b.rescheduled → morningBlocks=[], returns blocksToDelete=[], newBlocks=[]. Client hits line 44 and returns. No new blocks are ever written. Morning blocks remain stamped but the calendar shows nothing for the afternoon."
  },
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 86,
    "summary": "Guard reset on any error after calendar deletion allows a retry that sees an empty plan, leaving DB records orphaned",
    "failure_scenario": "archiveFocusBlocks throws after calendar events are already deleted. Catch resets RESCHEDULE_ATTEMPTED_KEY. Next mount: getActiveFutureBlocks returns the still-ACTIVE blocks, postSleepRecord returns blocksToDelete=[] (blocks are stamped rescheduled=true). Line 44 exits early. DB rows stay ACTIVE+rescheduled=true with no calendar events. No further recovery."
  },
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 61,
    "summary": "If all writeFocusBlock calls fail (permission revoked), morning blocks are already deleted with no replacement and no user notification",
    "failure_scenario": "Calendar permission revoked between app open and the reschedule write step. archiveFocusBlocks already ran (morning blocks deleted + archived). writeFocusBlock fails for all newBlocks via allSettled. toSave=[], saveFocusBlocks skipped, no SLEEP_SUMMARY_KEY update. User has 0 focus blocks for the day and sees no indication of failure."
  },
  {
    "file": "app/src/hooks/use-sleep-reschedule.ts",
    "line": 54,
    "summary": "Cancellation between calendar delete and archiveFocusBlocks leaves DB temporarily inconsistent (recovered by next calendar sync)",
    "failure_scenario": "cancelled=true fires after Promise.allSettled resolves (events deleted). Line 54 guard returns before archiveFocusBlocks. DB records stay ACTIVE, calendar events are gone. Recovered on next useCalendarSync run, but the window of inconsistency exists."
  }
]
```
