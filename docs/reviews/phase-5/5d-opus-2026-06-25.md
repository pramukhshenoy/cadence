# Phase 5d Opus Review — 2026-06-25

## Findings

```json
[
  {
    "file": "app/src/app/settings.tsx",
    "line": 89,
    "summary": "Sleep validation runs AFTER URL/token/model are already saved — any throw causes misleading 'Save failed' alert for a partially-successful save",
    "failure_scenario": "handleSave() saves URL (SecureStore), token (SecureStore), and model (backend) before reaching the sleep block. If hasInvalidSleepField throws, or if updateSleepSettings.mutateAsync rejects (e.g., server cross-field validation: sleepThreshold >= goodThreshold), the catch shows 'Could not save settings. Please try again.' — but URL/token/model were already durably written. User retries, re-saving those fields unnecessarily, with no indication which part failed. CONFIRMED."
  },
  {
    "file": "app/src/lib/health-connect.ts",
    "line": 97,
    "summary": "No guard against totalMs <= 0 from degenerate Health Connect sessions — displays '0.0h POOR' instead of hiding the card",
    "failure_scenario": "Health Connect can return sessions where endTime <= startTime (malformed records from some wearables). totalMs accumulates the delta unconditionally; durationHours = totalMs / 3_600_000 is 0 or negative. deriveSleepQuality returns POOR (0 < any threshold). Dashboard shows '0.0h Poor' with the red error color instead of suppressing the card. CONFIRMED."
  }
]
```

## Findings applied

1. **settings.tsx:89** — Moved sleep field parsing and validation to BEFORE `setSaveState('saving')` and any async saves. Added client-side cross-field check (threshold < good). Invalid sleep values now show a targeted alert with no side effects.

2. **health-connect.ts:97** — Added `if (totalMs <= 0) return null` guard before computing `durationHours`. Degenerate records return no sleep data instead of showing '0.0h POOR'.

## Refuted candidates

- **parseInt trailing garbage** (settings.tsx:84): REFUTED — Android numeric keyboard prevents letter input; paste-then-save edge case produces semantically correct truncation (leading digits = intent).
- **sleepInitialized ref freezes inputs**: REFUTED — server stores exact values; re-seeding after cache invalidation would produce identical state.
- **useSleepSync() never mounted**: REFUTED — `SleepSyncTrigger` in `_layout.tsx` mounts `useSleepSync()` unconditionally.
