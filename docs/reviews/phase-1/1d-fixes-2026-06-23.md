# Phase 1d — Fixes Summary (2026-06-23)

## Sonnet fixes applied

| Finding | File | Fix |
|---------|------|-----|
| Web tabs point to deleted explore route, missing 4 new tabs | app-tabs.web.tsx | Updated to 5 tabs (Dashboard/Tasks/Habits/Chat/Settings), renamed brand text to "Cadence" |
| Cannot clear a stored token | settings.tsx + api-client.ts | Added `deleteApiToken()`, handleSave now calls it when token field is cleared |
| async handleSave errors silently swallowed | settings.tsx | Wrapped in try/catch, shows Alert on failure |
| useEffect promise rejections unhandled | settings.tsx | Added .catch(() => {}) on both load promises |
| SecureStore reads on every request | api-client.ts | Module-level `cachedBaseUrl` and `cachedToken` caches; invalidated by setters |
| Timezone re-allocated every request | api-client.ts | Module-level `TIMEZONE` constant |
| useColorScheme from react-native not project wrapper | _layout.tsx | Changed to `@/hooks/use-color-scheme` |

## Opus fixes applied

| Finding | File | Fix |
|---------|------|-----|
| Silent .catch on token load → deleteApiToken wipes real token | settings.tsx | Added `tokenLoaded` flag; deleteApiToken only called when `tokenLoaded === true` |
| Content-Type unconditionally set on all requests | api-client.ts | Guard: only set when `options.body !== undefined && !headers.has('Content-Type')` |
| Double-tap concurrent saves | settings.tsx | Replaced `saved: boolean` with `saveState: 'idle'\|'saving'\|'saved'` state machine; button disabled while saving |

## Deferred

- API base URL stored in SecureStore instead of AsyncStorage — non-secret value, needs new dependency. Defer to Phase 3d settings refactor.
- In-flight promise memoization for cached reads — low severity for single-user app.
