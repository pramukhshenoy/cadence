# Phase 1c — Fixes Summary — 2026-06-23

## Sonnet Fixes

| # | File | Change |
|---|------|--------|
| S1 | `server/prisma/schema.prisma` | Added `@@unique([habitId, localDate])` to `HabitCompletion` — prevents duplicate completions for the same habit+date, which would silently corrupt streak counts |
| S2 | `server/prisma/migrations/.../migration.sql` | Added `INSERT OR IGNORE INTO "Settings" ...` — Settings singleton is now seeded by the migration itself, satisfying CLAUDE.md requirement and ensuring the row exists even with `prisma migrate deploy` |

## Opus Fixes

| # | File | Change |
|---|------|--------|
| O1 | `server/prisma/seed.ts` | **Deleted** — migration INSERT is the canonical seed; removing seed.ts also eliminates the `@updatedAt` silent-bump issue and the tsconfig/eslint exclusion |
| O2 | `server/package.json` | Removed `"prisma": { "seed": "..." }` — no longer needed (seed.ts deleted); also removes the Prisma 6 deprecation warning |
| O3 | `server/src/lib/settings.ts` | **Created** `getSettings(prisma)` helper — the CLAUDE.md-mandated upsert-on-read safety net; does `findUnique` first (no `updatedAt` touch on the happy path), upserts only if row is absent |
| O4 | `server/prisma/schema.prisma` | Added comment to `Settings.timezone`: distinguishes stored user preference from the live per-request `X-Timezone` header |

## Deferred

- `postinstall: prisma generate` fails on `npm install --omit=dev`: Acceptable for this project's single-machine deploy model; no documented workflow uses `--omit=dev`.

## Final State

- TypeScript: ✅ clean
- ESLint: ✅ clean
- Jest: ✅ pass (no tests expected for schema-only phase)
- Migration: applies cleanly on fresh DB, seeds Settings singleton
- Database: verified — Settings singleton row present with all defaults
