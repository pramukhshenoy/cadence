# Phase 1d — Sonnet Review (2026-06-23)

## Findings

```json
[
  {
    "file": "app/src/components/app-tabs.web.tsx",
    "line": 27,
    "summary": "Web tab component still references the deleted 'explore' route and is missing all 4 new tabs",
    "failure_scenario": "app-tabs.web.tsx has 2 tabs: 'home' and 'explore'. explore.tsx was deleted; the Explore tab is a dead link on web. Tasks, Habits, Chat, and Settings are unreachable on web."
  },
  {
    "file": "app/src/app/settings.tsx",
    "line": 32,
    "summary": "No path to clear a stored token — guard prevents call when field is empty, old token persists in SecureStore",
    "failure_scenario": "User clears token field and taps Save — guard `if (token.trim())` is falsy, setApiToken never called, stale token remains, every apiFetch still sends old Authorization header."
  },
  {
    "file": "app/src/app/settings.tsx",
    "line": 30,
    "summary": "async handleSave has no try/catch — SecureStore write failures become silent unhandled rejections",
    "failure_scenario": "If SecureStore throws (storage full, Keystore unavailable), setSaved(true) is never reached. User sees no error or confirmation. Unhandled rejection is silent in production."
  },
  {
    "file": "app/src/app/settings.tsx",
    "line": 23,
    "summary": "useEffect promise rejections are unhandled — SecureStore read failures on mount are silently swallowed",
    "failure_scenario": "getApiBaseUrl() or getApiToken() rejects on first launch or Keystore unavailability. No .catch() handler. Component retains defaults silently."
  },
  {
    "file": "app/src/lib/api-client.ts",
    "line": 29,
    "summary": "Two uncached SecureStore reads on every apiFetch call",
    "failure_scenario": "Phase 2+ nav events fire 5-10 queries simultaneously; each does 2 Keystore reads. Module-level caches (invalidated by setApiBaseUrl/setApiToken) would reduce to zero reads per subsequent request."
  },
  {
    "file": "app/src/lib/api-client.ts",
    "line": 7,
    "summary": "getTimezone() allocates new Intl.DateTimeFormat on every request; result is constant",
    "failure_scenario": "Module-level cached string eliminates repeated ICU object allocation."
  },
  {
    "file": "app/src/lib/api-client.ts",
    "line": 12,
    "summary": "API base URL stored in SecureStore (encrypted Keystore) — it is a config preference, not a secret",
    "failure_scenario": "SecureStore adds unnecessary Keystore overhead for a non-sensitive value. Ideally AsyncStorage; deferring to Phase 3d settings refactor."
  },
  {
    "file": "app/src/app/_layout.tsx",
    "line": 3,
    "summary": "useColorScheme from 'react-native' instead of project wrapper @/hooks/use-color-scheme",
    "failure_scenario": "If device returns 'unspecified', ThemeProvider gets unrecognised value while rest of app falls back to light — subtle theme mismatch."
  }
]
```

## Fixes to apply

- [x] app-tabs.web.tsx: update to 5 tabs matching native version
- [x] settings.tsx: add deleteApiToken path, try/catch in handleSave, .catch() in useEffect
- [x] api-client.ts: cache timezone and SecureStore reads at module level
- [x] _layout.tsx: use @/hooks/use-color-scheme
- [ ] URL in SecureStore → AsyncStorage: deferred (needs new dependency, low severity)
