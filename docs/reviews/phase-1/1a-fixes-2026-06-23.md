# Phase 1a — Fixes Summary
**Date:** 2026-06-23  
**Phase:** 1 — Foundation  
**Sub-phase:** 1a — Monorepo & Tooling

## Applied Fixes

| # | Source | File | Change |
|---|--------|------|--------|
| 1 | Sonnet | `server/eslint.config.js` | Replaced legacy `...tseslint.configs.recommended.rules` with `...tseslint.configs['flat/recommended']` (correct ESLint v9 flat preset); exempted config file from `no-require-imports` |
| 2 | Opus | `server/src/index.ts` | `PORT` type: `process.env.PORT ?? 3000` → `Number(process.env.PORT) \|\| 3000` (always `number`) |
| 3 | Opus | `server/src/index.ts` | Added `server.on('error', ...)` handler with EADDRINUSE diagnostic and `process.exit(1)` |
| 4 | Opus | `server/package.json` | Added `jest`, `ts-jest`, `@types/jest` to devDependencies; added `"test": "jest"` script |
| 5 | Opus | `server/jest.config.ts` | Created Jest config with `ts-jest` preset targeting `node` environment |
| 6 | Template fix | `app/src/types/declarations.d.ts` | Added CSS/CSS-module ambient declarations to fix Expo 56 template TypeScript errors |

## Deferred

| # | Issue | Deferred to |
|---|-------|------------|
| 1 | `app/src/types/declarations.d.ts` — overly broad CSS module declarations | Revisit if non-web CSS imports appear |
| 2 | `root package.json` — `lint:all` uses `&&`, masking server errors when app fails | Phase 6 or if CI becomes a pain point |
| 3 | `server/src/index.ts` — No `SIGTERM`/`SIGINT` graceful shutdown | Phase 1c (when Prisma is added) |

## Post-fix Quality Gate
- `tsc --noEmit` (app + server): ✅ PASS
- ESLint (app + server): ✅ PASS
