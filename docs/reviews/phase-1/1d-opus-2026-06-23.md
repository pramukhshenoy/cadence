# Phase 1d — Opus Review (2026-06-23)

## Findings (post-Sonnet fixes)

```json
[
  {
    "file": "app/src/app/settings.tsx",
    "line": 27,
    "summary": "Silent .catch(() => {}) on token load failure can cause handleSave to delete a valid stored token",
    "failure_scenario": "If getApiToken() rejects, the token field stays empty. User edits URL and hits Save; handleSave sees empty trimmedToken and calls deleteApiToken(), silently wiping the real stored token. Fix: track whether token loaded successfully; only delete when load succeeded and field is empty."
  },
  {
    "file": "app/src/lib/api-client.ts",
    "line": 45,
    "summary": "apiFetch unconditionally sets Content-Type: application/json on all requests — overrides caller value, wrong for GETs",
    "failure_scenario": "Any future multipart or non-JSON request will have its Content-Type clobbered. GETs and DELETEs get an unnecessary header. Fix: only set when body is present and caller has not already set it."
  },
  {
    "file": "app/src/app/settings.tsx",
    "line": 45,
    "summary": "No in-progress guard on Save button — rapid taps run handleSave concurrently, issuing overlapping SecureStore writes",
    "failure_scenario": "Double-tap fires two concurrent handleSave calls; both reach setApiBaseUrl/setApiToken simultaneously, producing concurrent Keystore writes. Fix: track saving state, disable button while write is in flight."
  },
  {
    "file": "app/src/lib/api-client.ts",
    "line": 13,
    "summary": "Caches memoize resolved values but not in-flight promises — concurrent first calls each issue their own SecureStore read",
    "failure_scenario": "On cold start, multiple apiFetch calls fire before cache is populated; each issues its own SecureStore.getItemAsync rather than sharing the one in-flight read. Low severity — not incorrect, just defeats cache purpose on cold start. Deferred."
  }
]
```

## Fixes to apply

- [x] settings.tsx: track tokenLoaded flag; skip deleteApiToken when initial load failed
- [x] api-client.ts: guard Content-Type — only set when body present and not already set
- [x] settings.tsx: saveState machine ('idle'|'saving'|'saved') to prevent double-tap concurrent writes
- [ ] api-client.ts: in-flight promise memoization — deferred (low severity for single-user app)
