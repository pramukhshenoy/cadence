# Phase 2c Fixes Summary — 2026-06-23

## Sonnet fixes (3)
1. task-item.tsx — removed `onBlur={saveEdit}` that caused save-on-cancel
2. lib/tasks.ts — simplified redundant `res.status !== 204` check in useDeleteTask
3. tasks.tsx — changed bottom padding from `Spacing.six` to `BottomTabInset + Spacing.three`

## Opus fixes (4)
4. task-item.tsx — moved `swipeable.current?.close()` before direction branch (covers delete path)
5. task-item.tsx — added `toLocalMidnight()` helper; normalized ISO dates in `isOverdue`/`formatDue`
6. task-item.tsx — added `enabled={!editing}` to disable swipe while inline edit is open
7. task-add.tsx — changed empty-submit from `dismiss()` to early `return` (keep form open)
