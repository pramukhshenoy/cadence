# Phase 2d — Fixes Summary
Date: 2026-06-23

## Source: Sonnet review
No fixes required.

## Source: Opus review

### O1 — Added tests for DELETE /complete [FIXED]
File: `server/src/__tests__/habits.test.ts`
- Added `deleteMany: jest.fn()` to the `habitCompletion` mock.
- Added `describe('DELETE /api/habits/:id/complete')` block with 4 tests:
  - success (204) + verifies deleteMany called with correct args
  - idempotent when no completion existed (204, count: 0)
  - 404 when habit not found
  - 401 without auth
- Tests: 73 → 77, all passing.

### O3 — Optimistic toggle updates [FIXED]
File: `app/src/lib/habits.ts`
- Both `useCompleteHabit` and `useUncompleteHabit` now use `onMutate` to cancel in-flight queries, snapshot previous data, and immediately flip `completedToday` in the cache.
- `onError` rolls back to the snapshot.
- `onSettled` invalidates to sync with server truth.
- Effect: checkmark flips instantly on tap; rolls back if the network call fails.

### O4 — Streak label pluralization [FIXED]
File: `app/src/components/habit-item.tsx`
- "2 day streak" → "2 days streak"; "1 day streak" stays "1 day streak".
- Same for weekly: "2 week streak" → "2 weeks streak".

## Deferred (separate tracking)
- O2: Task due-date comparison uses JS local time vs. X-Timezone contract — pre-existing pattern, cross-cutting fix needed in a future cleanup.
- O5: `weeklyTargetDays` typed as raw JSON string in client type — harmless now, revisit when UI needs to display target days.

## Final test counts
- Server: 77 tests, all passing
- App tsc: 0 errors
- ESLint: 0 errors
