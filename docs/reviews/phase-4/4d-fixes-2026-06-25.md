# Phase 4d — Fixes Applied

Date: 2026-06-25

## From Sonnet review

1. **Shortfall display threshold** (`app/src/app/index.tsx:134`)
   - Changed `shortfallHours > 0` to `shortfallHours >= 0.1` to prevent "0.0h shortfall" from tiny float noise.

2. **Parallel DB calls** (`server/src/routes/focus-blocks.ts`)
   - Replaced sequential `await getSettings` + `await findMany` with `Promise.all([...])`.

3. **Invalidate after sync** (`app/src/hooks/use-calendar-sync.ts`)
   - Added `queryClient.invalidateQueries({ queryKey: FOCUS_WEEK_SUMMARY_KEY })` in `use-calendar-sync.ts` after focus blocks are saved.

## From Opus review

4. **Cache invalidation on all sync exit paths** (`app/src/hooks/use-calendar-sync.ts`)
   - Added invalidation before the `focusBlocks.length === 0` early return.
   - Moved final invalidation outside the `if (toSave.length > 0)` guard so it fires even when all writes fail.

5. **Test coverage for `/week-summary`** (`server/src/__tests__/focus-blocks.test.ts`)
   - Extended Prisma mock to include `settings.findUnique` / `settings.upsert`.
   - Added 5 tests with `jest.useFakeTimers` pinned to `2026-06-25T10:00:00.000Z`.
   - Tests: 401 without auth, zero blocks, past block counts as elapsed, future block not elapsed, full target → zero shortfall, ACTIVE filter check.
   - Total tests: 149 → 155, all passing.
