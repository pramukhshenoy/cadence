# Phase 4c Fixes Summary — 2026-06-24

## Sonnet Review Fixes

| # | Finding | File | Change |
|---|---------|------|--------|
| S1 | `archiveFocusBlocks` called unconditionally | `use-calendar-sync.ts` | Changed to only archive IDs where `deleteCalendarEvent` succeeded (filter by `status === 'fulfilled'`). *(Superseded by Opus fix O1.)* |
| S2 | Invalid ISO date strings pass batch validation | `focus-blocks.ts` (server) | Added `!isNaN(new Date(block.startTime).getTime())` and same for `endTime` to the filter predicate |
| S3 | `new Date(null)` injects epoch for all-day events | `calendar.ts` | Added `.filter((e) => e.startDate != null && e.endDate != null)` before mapping events |
| S4 | `hasRunRef` blocked re-sync on calendar change | `use-calendar-sync.ts` | Removed `hasRunRef` entirely; `useEffect` dependency array already handles re-triggering |
| S5 | `getActiveFutureBlocks` + `getSourceCalendarIds` sequential | `use-calendar-sync.ts` | Merged into `Promise.all([...])` |

## Opus Review Fixes

| # | Finding | File | Change |
|---|---------|------|--------|
| O1 | Zombie ACTIVE blocks + unbounded accumulation (C1+C2) | `use-calendar-sync.ts` | Reverted to archiving ALL blocks regardless of delete result; delete failures indicate event already gone (correct to archive) |
| O2 | No cancellation guard — concurrent syncs in Strict Mode (C3) | `use-calendar-sync.ts` | Added `let cancelled = false;` + `return () => { cancelled = true; }` cleanup; `if (cancelled) return;` guards after each major await |
| O3 | Silent catch discards all errors (C4) | `use-calendar-sync.ts` | Changed to `catch((err: unknown) => { if (!cancelled) console.error('[CalendarSync]', err); })` |
| O4 | `archiveFocusBlocks` + `readWeekEvents` sequential (C5) | `use-calendar-sync.ts` | Parallelised with `Promise.all([archiveFocusBlocks(...), readWeekEvents(...)])` |
| O5 | `toSave` for-loop inconsistent with functional pattern (C6) | `use-calendar-sync.ts` | Converted to `focusBlocks.flatMap((b, i) => ...)` |
| O6 | `BlockInput` type at function scope (C7) | `focus-blocks.ts` (server) | Moved `type BlockInput` to file scope |

## Post-Fix Quality Gate

- **Tests**: 149/149 pass (6 suites)
- **TypeScript**: 0 errors (app + server)
- **ESLint**: 0 warnings/errors (app + server)
