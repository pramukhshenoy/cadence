# Phase 4b Sonnet Review — 2026-06-24

## Files Reviewed
- `server/src/lib/scheduler.ts` (new)
- `server/src/routes/calendar.ts` (new)
- `server/src/__tests__/calendar.test.ts` (new)
- `server/src/app.ts` (modified)

## Findings

```json
[
  {
    "file": "server/src/lib/scheduler.ts",
    "line": 104,
    "summary": "remainingMinutes < MIN_BLOCK_MINUTES causes all remaining gaps/days to spin uselessly",
    "failure_scenario": "focusHoursPerWeek=1, first gap is 45 min → blockMin=45, remainingMinutes=15; outer day/gap loops never break (guard only checks <=0), every subsequent gap hits blockMin=15<30 and breaks immediately; result correct (shortfall reported) but algorithm iterates all remaining days doing nothing"
  },
  {
    "file": "server/src/lib/scheduler.ts",
    "line": 65,
    "summary": "localTimeToUtc gives wildly wrong result for times in DST spring-forward gap",
    "failure_scenario": "localTimeToUtc('2024-03-10', 2, 30, 'America/New_York') — non-existent local time; pivot at 02:30 UTC is 21:30 EST (prior day), errorMin=1140, result shifts 19 hours backward. Only affects hours in the 2–3 AM gap; workday defaults (9/18) never trigger this."
  },
  {
    "file": "server/src/lib/scheduler.ts",
    "line": 37,
    "summary": "utcToLocalDateStr duplicates todayLocalDate from habits.ts (three copies total)",
    "failure_scenario": "Fix to locale/format options must be applied in scheduler.ts, habits.ts, and chat.ts independently; no crash risk today."
  }
]
```

## Status: Applying confirmed fix (#1)
